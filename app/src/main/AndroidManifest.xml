<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.carlosllerenatest.reactiveprogrammingfull">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".RX09HotAndCold"></activity>
        <activity android:name=".RX08BackPressureActivity" />
        <activity android:name=".RX07BindingAcitivity" />
        <activity android:name=".RX06BusActivity" />
        <activity android:name=".RX05SubjectActivity" />
        <activity android:name=".RX04TiposObservableActivity" />
        <activity android:name=".RX03Operadores" />
        <activity android:name=".RX02CompositeDisposableActivity" />
        <activity android:name=".RX01DisposableActivity" />
        <activity android:name=".RX00IntroActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>